class iphone14pro:
    
    
    def __init__(self, proccesser, memory, camera, battery):
        self.proccesser = proccesser
        self.memory = memory
        self. camera = camera
        self.battery = battery

print(f'Firstname {michael}')




class user:

    def __init__(self, name, email_address, account_balance):
        self.name = 'Michael'
        self.email_address = 'michael@codingdodojo.com'
        self.account_balance = '0'

    def user_info(self):
        return ()

guido = user(f 'First Name' {michael})
monty = user()

guido.name = 'Guido'
print(guido.name)
monty.name = 'Monty'
print(monty.name)










class Department():

    def __init__(self, name, location):
        self.name = name
        self.location = location
        self.employees = []




department_a = Department('Sales, '804A')


self.department.employees.append(self) # we can connect this to a diffrent vlaue in a class